Addan:
  - Give a class name to the unnamed div in .rowInput
  - Fill up all subject marks/weightages with guesses for now
  - Cleanup JS
  - Help me think of a good site name (Current is "gpacalculator-fall2022")

  - (Done) Fix subject orders (Alphabetically)
  - (Done) Implement "Add Subject" as default item in select menu. Also choose it back again after adding/removing subjects
  - (Done) Add tooltip to weightage & update max marks tooltip
  - (Done) Add toFixed(1) to subject marks
  - (Done) Trigger .popup-theme-selector with button instead of "Enter"
  - (Done) Add functionality to pop-up closing button
  - (Done) Hide SGPA when subject is added or removed
  - (Done) Add weightage after labels
  - (Done) Since theme preference is saved, sync the checked radio-button with current theme after refresh/revisit

Asad:
  - Design & populate usage popup
  - Improve/Fix Design
